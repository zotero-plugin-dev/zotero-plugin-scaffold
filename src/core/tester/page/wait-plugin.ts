/**
 * tsc placeholder — the bundler replaces this module with the configured
 * waitForPlugin expression (see the tester-page-manifest plugin).
 */
export const waitForPluginReady: (() => boolean) | null = null;
